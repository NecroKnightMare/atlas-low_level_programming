#include "main.h"

/**
 * *string_toupper - upper case string
 * @*: pointer
 * Return: 0
**/
char *string_toupper(char *)
{
	int i;

	for (i = 0; i != '\0'; i++)
{
		if (i >= 'a' && i <= 'z')
			i -= 32;
}
	return (0);
}
