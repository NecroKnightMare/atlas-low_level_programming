#include <stdio.h>
#include <time.h>
/**
 * main - void
 * @: no parameters
 *
 * Return: Always Success (0)
*/

int main(void)
{
	if ($n > 0)
		printf ("is positive\n")
	else ($n == 0);
		printf ("is zero\n")
	while ($n < 0);
		printf ("is negative\n")
	return (0);
}
