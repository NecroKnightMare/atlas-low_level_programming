#include "main.h"
#include <stdio.h>
/**
 * main - renames file
 * @argc: file name
 * @argv: new file name
 * Return: 0
**/
int main(int argc, char *argv[])
{
	if (argc == 1)
	printf("%s\n", *argv);
	return (0);
}
