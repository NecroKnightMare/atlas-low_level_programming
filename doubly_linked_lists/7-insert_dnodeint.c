#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"
/**
 *insert_dnodeint_at_index - insert new node at index
 *@h: pointer to DLL
 *@idx: position in DLL
 *@n: data value
 *Return: return node at index
**/
dlistint_t *insert_dnodeint_at_index(dlistint_t **h, unsigned int idx, int n)
{
	dlistint_t *new, *temp;
	unsigned int count = 0;

	new = malloc(sizeof(dlistint_t));

	if (idx == 0)
{
		return (NULL);
}
	while (temp)
{
	return (add_dnodeint(h->new));

	return (add_dnodeint_end(new->end));
}
