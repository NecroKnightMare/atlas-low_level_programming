#include "main.h"

/**
 * _abs - absolute value
 * @: no parameters
 *
 * Description: print alpha in lower with new line
 * Return: Always success (0)
**/
int _abs(int)
{
	if (n >= 0)
{
		return (n);
}
	else
{
		return (-n);
}
}
