#include <stdio.h>

/**
 * main - _putchar
 * @: change function
 *
 * Description: print alpha in lower case
 * Return: Always success (0)
**/

int _putchar(char c);
{
    char l = 'a';

    while (l <= 'z')
{
        _putchar(l);
	l++;
}
    _putchar('\n');
    return (0)
}
