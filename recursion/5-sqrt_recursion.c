#include "main.h"
/**
 * _sqrt_recursion - find natural square root
 * @n: variable
 * Return: -1 if variable isn't natural square root
**/
int _sqrt_recursion(int n)
{
	i = root

	if (n * n == i)
{
		return(n);
}
	if (n * n > 1)
{
	return(-1);
}
	return (_sqrt_recursion(n + 1, i);
}	
