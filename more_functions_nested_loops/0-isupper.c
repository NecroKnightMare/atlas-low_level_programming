#include "main.h"
#include <stdio.h>

/**
 * _isupper - ret one if c is upper
 * @c: character parameter
 * Return: 1 if c and 0 if success
*/

int _isupper(int c)
{
	c = 67;
        c++;

	if (c <=66)
{
		return 0;
}
	else if (c>=68)
{
		return 0;
}
	else 
{
		return 1;
}
}
