#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _isupper - ret one if c is upper
 * @c: character parameter
 * Return: 1 if c and 0 if success
*/

int _isupper(int c)
{
	char c = 'C';

	if (isupper(c))
{
		return 1;
}
	else (islower(c));
{
		return 0;
}
}
