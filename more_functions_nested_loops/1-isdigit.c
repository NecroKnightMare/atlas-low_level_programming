#include "main.h"

/**
 * _isdigit - 0-9 
 * @c: integer
 * Returns: 1 if digit o otherwise
*/

int _isdigit(int c)
{
        for (c = 0; c <= 9; c++)
{
	return 1;
                if (c != 0) && (c !<= 9)
{
                        return 0;
}
}
